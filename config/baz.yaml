apiVersion: eventing.knative.dev/v1alpha1
kind: Trigger
metadata:
  name: baz-trigger
  namespace: default
spec:
  filter:
    sourceAndType:
      type: dev.mattmoor.knobots.baz
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1beta1
      kind: Service
      name: baz-bot
---
apiVersion: serving.knative.dev/v1beta1
kind: Service
metadata:
  name: baz-bot
  labels:
    serving.knative.dev/visibility: cluster-local
spec:
  template:
    spec:
      containers:
      - image: github.com/mattmoor/knobots/cmd/baz
