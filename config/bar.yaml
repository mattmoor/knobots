apiVersion: eventing.knative.dev/v1alpha1
kind: Trigger
metadata:
  name: bar-trigger
  namespace: default
spec:
  filter:
    sourceAndType:
      type: dev.mattmoor.knobots.bar
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1beta1
      kind: Service
      name: bar-bot
---
apiVersion: serving.knative.dev/v1beta1
kind: Service
metadata:
  name: bar-bot
  labels:
    serving.knative.dev/visibility: cluster-local
spec:
  template:
    spec:
      containers:
      - image: github.com/mattmoor/knobots/cmd/bar
